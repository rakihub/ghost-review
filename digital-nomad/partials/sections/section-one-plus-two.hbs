 <section class="section-one-plus-two{{class}}">
    <div class="wrapper">
        {{#match tag "=" 'show-latest-posts'}}
            <h2 class="layout-title">{{t "Latest Posts"}}</h2>
            <div class="layout-flex">
                {{#get "posts" limit = 3 include="tags,authors" order="published_at desc"}}
                    {{#foreach posts}}

                        {{#has index="1"}}
                            <div class="layout-aside layout-aside--two-posts">
                        {{/has}}

                        {{#if @first}}
                            {{>post-cards/post-card postType="Big post" showExcerpt="true" lazyLoading=../../lazyLoading}}
                        {{else}}
                            {{>post-cards/post-card postType="Small post" showExcerpt="true" lazyLoading=../../lazyLoading}}
                        {{/if}}

                        {{#has index="2"}}
                            </div>
                        {{/has}}
                    
                    {{/foreach}}
                {{/get}}
            </div>
        {{else match tag "=" 'show-featured-posts'}}
            <h2 class="layout-title">{{t "Featured Posts"}}</h2>
            <div class="layout-flex">
                {{#get "posts" limit = 3 include="tags,authors" order="published_at desc" filter="featured:true"}}
                    {{#foreach posts}}

                        {{#has index="1"}}
                            <div class="layout-aside layout-aside--two-posts">
                        {{/has}}

                        {{#if @first}}
                            {{>post-cards/post-card postType="Big post" showExcerpt="true" lazyLoading=../../lazyLoading}}
                        {{else}}
                            {{>post-cards/post-card postType="Small post" showExcerpt="true" lazyLoading=../../lazyLoading}}
                        {{/if}}

                        {{#has index="2"}}
                            </div>
                        {{/has}}
                    
                    {{/foreach}}
                {{/get}}
            </div>
        {{else}}
            <h2 class="layout-title">
                <a class="layout-title__link" href="/tag/{{tag}}" arial-label="{{t "Link to page tag {tagName}" tagName=tag}}">
                    {{#get "tags" filter="slug:{{tag}}"}}
                        {{#foreach tags}}
                        {{name}}
                        {{/foreach}}
                    {{/get}}
                </a>
            </h2>
            <div class="layout-flex">
                {{#get "posts" limit = 3 include="tags,authors" order="published_at desc" filter="primary_tag:{{tag}}"}}
                    {{#foreach posts}}

                        {{#has index="1"}}
                            <div class="layout-aside layout-aside--two-posts">
                        {{/has}}

                        {{#if @first}}
                            {{>post-cards/post-card postType="Big post" showExcerpt="true" lazyLoading="true" lazyLoading=../../lazyLoading}}
                        {{else}}
                            {{>post-cards/post-card postType="Small post" showExcerpt="true" lazyLoading="true" lazyLoading=../../lazyLoading}}
                        {{/if}}

                        {{#has index="2"}}
                            </div>
                        {{/has}}
                    
                    {{/foreach}}
                {{/get}}
            </div>
        {{/match}}      
    </div>
</section>